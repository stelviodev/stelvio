"""
Stub Lambda handler - forwards invocations to local dev server via AppSync.
Uses connection reuse for better performance.
"""

import json

def handler(event, context):
    """Debug only."""
    return {"statusCode": 200, "body": json.dumps({"error": "Stub function"})}
